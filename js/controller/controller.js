import { firstView } from "./../view/first.js";
import { secondView } from "../view/second.js";
import { thirdView } from "../view/third.js";
import { fourthView } from "../view/fourth.js";
import { fiveView } from "../view/five.js";
import { sixthView } from "../view/sixth.js";
import { seventhView } from "../view/seventh.js";
import { eighthView } from "../view/eighth.js";
import { ninethView } from "../view/nineth.js";
import { tenthView } from "../view/tenth.js";

export let controller_first = {
    init: (getInput) => {
        firstView.show();
    }
}

export let controller_second = {
    init: (getInput) => {
        secondView.show();
    }
}

export let controller_third = {
    init: (getInput) => {
        thirdView.show();
    }
}

export let controller_fourth = {
    init: (getInput) => {
        fourthView.show();
    }
}

export let controller_five = {
    init: (getInput) => {
        fiveView.show();
    }
}

export let controller_sixth = {
    init: (getInput) => {
        sixthView.show();
    }
}

export let controller_seventh = {
    init: (getInput) => {
        seventhView.show();
    }
}

export let controller_eighth = {
    init: (getInput) => {
        eighthView.show();
    }
}

export let controller_nineth = {
    init: (getInput) => {
        ninethView.show();
    }
}

export let controller_tenth = {
    init: (getInput) => {
        tenthView.show();
    }
}
